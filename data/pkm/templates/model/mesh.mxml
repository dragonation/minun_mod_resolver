<m3d-mesh id="mesh-${index}-${mesh.name}"
          name="${mesh.name}"
          skeleton="model-skeleton"
          materials="${mesh.material}"
          indices="@meshes/${index}-${mesh.name}/indices.vertices.ui16.bin"
          vertices="@meshes/${index}-${mesh.name}/positions.f32.bin"
          normals="@meshes/${index}-${mesh.name}/normals.f32.bin"
          tangents="@meshes/${index}-${mesh.name}/tangents.f32.bin"
          colors="@meshes/${index}-${mesh.name}/colors.f32.bin"
          uvs="@meshes/${index}-${mesh.name}/uvs[0].f32.bin"
          uvs-2="@meshes/${index}-${mesh.name}/uvs[1].f32.bin"
          uvs-3="@meshes/${index}-${mesh.name}/uvs[2].f32.bin"
          bone-indices="@meshes/${index}-${mesh.name}/bone.indices.f32.bin"
          bone-weights="@meshes/${index}-${mesh.name}/bone.weights.f32.bin"/>